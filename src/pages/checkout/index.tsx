import Checkout from '@/features/checkout/components/checkout';

/**
 * Checkout Page
 *
 * Handles the checkout process for the shopping cart.
 * Uses the Checkout feature component to manage order submission.
 */
const CheckoutPage = () => {
  return <Checkout />;
};

export default CheckoutPage;
